import { getSession, getWorkspaceContext, supabase } from './backend.js';

const app=document.querySelector('#app');
const pageTitle=document.querySelector('#pageTitle');
const esc=(v='')=>String(v).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'})[c]);
let session=null,context=null,loading=false;

function icon(kind){return({accepted_estimate:'✓',overdue_invoice:'!',estimate_viewed:'↗',hot_lead:'★',estimate_no_reply:'↺',stale_customer:'◷'})[kind]||'→'}
function tone(kind){return kind==='accepted_estimate'||kind==='hot_lead'?'strong':kind==='overdue_invoice'?'danger':'normal'}
async function loadActions(){const{data,error}=await supabase.rpc('get_crm_next_actions');if(error)throw error;return data||[]}
function style(){if(document.querySelector('#crmNextActionStyle'))return;const s=document.createElement('style');s.id='crmNextActionStyle';s.textContent='.nba-card{margin-top:18px}.nba-head{display:flex;align-items:flex-start;justify-content:space-between;gap:12px}.nba-list{display:grid;gap:9px;margin-top:14px}.nba-item{display:grid;grid-template-columns:34px minmax(0,1fr) auto;gap:10px;align-items:center;padding:12px;border:1px solid #e7eaf0;border-radius:13px;background:#fff}.nba-item.strong{border-color:#d7e2ff;background:#f8faff}.nba-item.danger{border-color:#f2cccc;background:#fffafa}.nba-icon{width:32px;height:32px;display:grid;place-items:center;border-radius:10px;background:#f3f5f8;font-weight:800}.nba-copy strong,.nba-copy small{display:block}.nba-copy small{margin-top:3px;color:#697386;font-size:12px}.nba-rank{font-size:11px;color:#8791a2}.nba-item .mini-btn{white-space:nowrap}@media(max-width:650px){.nba-item{grid-template-columns:34px 1fr}.nba-item .mini-btn{grid-column:1/-1;width:100%}}';document.head.appendChild(s)}
function render(actions){if(pageTitle?.textContent!=='Dashboard'||app.querySelector('.nba-card'))return;const target=app.querySelector('.today-center')||app.querySelector('.dashboard-v2')||app.querySelector('.stats');if(!target)return;const wrap=document.createElement('section');wrap.className='card nba-card';wrap.innerHTML=`<div class="nba-head"><div><p class="eyebrow">NEXT BEST ACTION</p><h2>What to do next</h2><p class="muted">SoloBizKit ranks the next actions from customer activity, estimates, invoices and pipeline signals.</p></div>${context?.canAdmin?'<a class="mini-btn" href="/pro/pipeline/?rules=1">Automation rules</a>':''}</div><div class="nba-list">${actions.length?actions.map((a,i)=>`<div class="nba-item ${tone(a.action_kind)}"><div class="nba-icon">${icon(a.action_kind)}</div><div class="nba-copy"><strong>${esc(a.title)}</strong><small>${esc(a.detail||'')}</small><span class="nba-rank">Priority #${i+1}</span></div><a class="mini-btn" href="${esc(a.action_href||'/pro/pipeline/')}">${esc(a.action_label||'Open')}</a></div>`).join(''):'<div class="empty compact-empty">No urgent next action right now.</div>'}</div>`;target.insertAdjacentElement('afterend',wrap)}
async function enhance(){if(loading||pageTitle?.textContent!=='Dashboard')return;loading=true;try{style();session||=await getSession();if(!session?.user?.id)return;context=await getWorkspaceContext();const actions=await loadActions();render(actions)}catch(e){console.warn('Next best action skipped',e)}finally{loading=false}}
new MutationObserver(()=>requestAnimationFrame(enhance)).observe(app,{childList:true,subtree:true});
window.addEventListener('focus',()=>{document.querySelector('.nba-card')?.remove();enhance()});
window.addEventListener('solobizkit:workspace-updated',()=>{document.querySelector('.nba-card')?.remove();enhance()});
enhance();