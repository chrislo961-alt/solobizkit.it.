import { calculateInvoice, money } from '../pro-core.js';
import { convertEstimateToInvoice, getCompanySettings, getSession, loadWorkspace, onAuthChange, saveEstimate, signIn, signOut, signUp, supabase } from '../backend.js';

const app = document.querySelector('#app');
const shell = document.querySelector('#shell');
const authActions = document.querySelector('#authActions');
const newEstimateButton = document.querySelector('#newEstimate');
const modal = document.querySelector('#modal');
const modalForm = document.querySelector('#modalForm');
const modalTitle = document.querySelector('#modalTitle');
const modalBody = document.querySelector('#modalBody');
let session = null, subscription = null, workspaceContext = null, modalAction = null;
let state = { customers: [], invoices: [], estimates: [], settings: { currency:'USD', taxRate:0, estimatePrefix:'EST-' } };

function esc(value=''){return String(value).replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#039;','"':'&quot;'})[c]);}
function todayISO(){return new Date().toISOString().slice(0,10);}
function plusDaysISO(days){const d=new Date();d.setDate(d.getDate()+Math.max(0,Number(days||0)));return d.toISOString().slice(0,10);}
function normalizedPrefix(value,fallback='EST-'){const raw=String(value||fallback).trim().replace(/\s+/g,'');return raw.endsWith('-')?raw:`${raw}-`;}
function hasProAccess(){return ['active','trialing'].includes(String(subscription?.status||'').toLowerCase())&&String(subscription?.plan||'').toLowerCase()==='pro';}
function canWrite(){return Boolean(workspaceContext?.canWrite);}
function customerName(id){return state.customers.find(c=>c.id===id)?.name||'Unknown customer';}
function nextEstimateNumber(){const used=state.estimates.map(e=>Number(String(e.number||'').match(/(\d+)$/)?.[1]||0));return `${state.settings.estimatePrefix}${String(Math.max(1000,...used)+1).padStart(4,'0')}`;}
function setBusy(message){app.innerHTML=`<div class="auth-stage"><div class="auth-card"><p class="eyebrow">SOLOBIZKIT PRO</p><h2>${esc(message)}</h2></div></div>`;}
function showError(error){console.error(error);alert(error?.message||'Something went wrong.');}

async function hydrate(){
  if(!session?.user?.id)return;
  setBusy('Loading estimates…');
  const [workspace,settings]=await Promise.all([loadWorkspace(session.user.id),getCompanySettings(session.user.id)]);
  state={customers:workspace.customers.map(x=>({...x,persisted:true})),invoices:workspace.invoices.map(x=>({...x,persisted:true})),estimates:workspace.estimates.map(x=>({...x,persisted:true})),settings:{currency:settings.defaultCurrency||'USD',taxRate:Number(settings.defaultTax||0),estimatePrefix:normalizedPrefix(settings.estimatePrefix,'EST-')}};
  subscription=workspace.subscription;workspaceContext=workspace.workspace||null;renderAccount();render();
}
function renderAccount(){const label=workspaceContext?.role?String(workspaceContext.role).toUpperCase():(hasProAccess()?'PRO':'ACCOUNT');authActions.innerHTML=`<span class="account-chip"><strong>${esc(session?.user?.email||'Account')}</strong><small>${esc(label)}</small></span><button class="mini-btn" id="signOutBtn">Sign out</button>`;authActions.querySelector('#signOutBtn').onclick=()=>signOut().catch(showError);}
function renderAuth(){shell.classList.add('signed-out');newEstimateButton.hidden=true;authActions.innerHTML='';app.innerHTML=`<div class="auth-stage"><section class="auth-card"><p class="eyebrow">SOLOBIZKIT PRO</p><h2>Sign in to manage estimates.</h2><p class="muted auth-copy">Quotes and estimates stay synced with your customers and invoices.</p><form id="authForm" class="auth-form"><label>Email<input class="input" name="email" type="email" required></label><label>Password<input class="input" name="password" type="password" minlength="8" required></label><button class="btn primary" type="submit">Sign in</button><button class="btn secondary" id="createAccount" type="button">Create account</button><p class="auth-message" id="authMessage"></p></form></section></div>`;const form=app.querySelector('#authForm'),message=app.querySelector('#authMessage');form.onsubmit=async e=>{e.preventDefault();try{await signIn(form.email.value.trim(),form.password.value)}catch(error){message.textContent=error.message}};app.querySelector('#createAccount').onclick=async()=>{if(!form.reportValidity())return;try{const data=await signUp(form.email.value.trim(),form.password.value);message.textContent=data.session?'Account created.':'Check your email, then sign in.'}catch(error){message.textContent=error.message}};}
async function startCheckout(billing){if(!workspaceContext?.isOwner)return showError(new Error('Only the workspace owner can manage the subscription.'));try{const{data,error}=await supabase.functions.invoke('create-solobizkit-checkout',{body:{billing}});if(error)throw error;if(data?.alreadyPro)return hydrate();if(!data?.url)throw new Error(data?.error||'Could not start checkout.');window.location.assign(data.url)}catch(error){showError(error)}}
function renderPaywall(){shell.classList.add('paywalled');newEstimateButton.hidden=true;const ownerNote=workspaceContext&&!workspaceContext.isOwner?'<div class="paywall-notice">Ask the workspace owner to manage billing for this workspace.</div>':'';app.innerHTML=`<div class="paywall-wrap"><section class="paywall-card"><p class="eyebrow">SOLOBIZKIT PRO</p><h2>Estimates are part of Pro.</h2><p class="paywall-lead">Create quotes, track acceptance and convert accepted work into invoices without retyping anything.</p>${ownerNote}<div class="paywall-features"><span>✓ Estimates and quotes</span><span>✓ Draft / Sent / Accepted tracking</span><span>✓ One-click invoice conversion</span><span>✓ Shared customer history</span></div>${workspaceContext?.isOwner!==false?'<div class="paywall-plans"><button class="paywall-plan" data-checkout="monthly"><small>MONTHLY</small><strong>Start Pro monthly</strong></button><button class="paywall-plan featured" data-checkout="annual"><small>ANNUAL</small><strong>Start Pro annually</strong></button></div>':''}</section></div>`;app.querySelectorAll('[data-checkout]').forEach(b=>b.onclick=()=>startCheckout(b.dataset.checkout));}

function render(){if(!session)return renderAuth();if(!hasProAccess())return renderPaywall();shell.classList.remove('signed-out','paywalled');newEstimateButton.hidden=!canWrite();const estimates=[...state.estimates].sort((a,b)=>String(b.createdAt||'').localeCompare(String(a.createdAt||'')));app.innerHTML=`<section class="card"><div class="toolbar"><input class="input search" id="estimateSearch" type="search" placeholder="Search estimate or customer…"><select class="select" id="estimateStatus" style="max-width:170px"><option value="">All statuses</option><option value="draft">Draft</option><option value="sent">Sent</option><option value="accepted">Accepted</option><option value="declined">Declined</option><option value="converted">Converted</option></select>${canWrite()?'<button class="btn primary" id="newEstimateInner">+ Estimate</button>':''}</div><div id="estimateResults"></div></section>`;const search=app.querySelector('#estimateSearch'),status=app.querySelector('#estimateStatus'),results=app.querySelector('#estimateResults');const update=()=>{const q=search.value.trim().toLowerCase();const filtered=estimates.filter(e=>!status.value||e.status===status.value).filter(e=>`${e.number} ${customerName(e.customerId)}`.toLowerCase().includes(q));results.innerHTML=filtered.length?`<div class="table-wrap"><table class="table"><thead><tr><th>Estimate</th><th>Customer</th><th>Valid until</th><th>Status</th><th>Total</th><th></th></tr></thead><tbody>${filtered.map(estimateRow).join('')}</tbody></table></div>`:'<div class="empty">No estimates found.</div>';bindActions(results)};search.oninput=update;status.onchange=update;app.querySelector('#newEstimateInner')?.addEventListener('click',()=>openEstimateModal());update();}

function estimateRow(estimate){const total=calculateInvoice({lines:estimate.lines,taxRate:estimate.taxRate}).total;const converted=estimate.status==='converted'||estimate.convertedInvoiceId;let actions='';if(canWrite()&&!converted)actions=`<button class="mini-btn" data-edit="${esc(estimate.id)}">Edit</button> <button class="mini-btn" data-convert="${esc(estimate.id)}">Convert to invoice</button>`;else if(!canWrite()&&!converted)actions='<span class="muted">Read only</span>';if(converted&&estimate.convertedInvoiceId)actions=`<a class="mini-btn" href="/pro/?view=invoices&invoice=${encodeURIComponent(estimate.convertedInvoiceId)}">Open invoice</a>`;return `<tr data-estimate-id="${esc(estimate.id)}"><td><strong>${esc(estimate.number)}</strong><br><span class="muted">${esc(estimate.issueDate||'')}</span></td><td>${esc(customerName(estimate.customerId))}</td><td>${esc(estimate.validUntil||'—')}</td><td><span class="estimate-status ${esc(estimate.status)}">${esc(estimate.status)}</span></td><td>${money(total,estimate.currency)}</td><td><div class="estimate-actions" data-estimate-id="${esc(estimate.id)}">${actions}</div></td></tr>`;}
function bindActions(root){root.querySelectorAll('[data-edit]').forEach(b=>b.onclick=()=>openEstimateModal(b.dataset.edit));root.querySelectorAll('[data-convert]').forEach(b=>b.onclick=async()=>{if(!canWrite())return showError(new Error('This workspace role is read only.'));const estimate=state.estimates.find(x=>x.id===b.dataset.convert);if(!estimate)return;b.disabled=true;b.textContent='Converting…';try{const invoice=await convertEstimateToInvoice(session.user.id,estimate);state.invoices.unshift(invoice);state.estimates=state.estimates.map(x=>x.id===estimate.id?{...x,status:'converted',convertedInvoiceId:invoice.id}:x);window.location.assign(`/pro/?view=invoices&invoice=${encodeURIComponent(invoice.id)}`)}catch(error){showError(error);render()}});}

function lineMarkup(line={description:'',qty:1,rate:0}){return `<div class="estimate-line"><input class="input" name="description" placeholder="Description" value="${esc(line.description||'')}"><input class="input" name="qty" type="number" min="0.01" step="0.01" value="${Number(line.qty)||1}"><input class="input" name="rate" type="number" min="0" step="0.01" value="${Number(line.rate)||0}"><button class="mini-btn remove-line" type="button">×</button></div>`;}
function openEstimateModal(estimateId=null){if(!canWrite())return showError(new Error('This workspace role is read only.'));if(!state.customers.length){alert('Add a customer first from the Pro dashboard.');return;}const existing=state.estimates.find(e=>e.id===estimateId);const estimate=existing||{customerId:state.customers[0].id,number:nextEstimateNumber(),issueDate:todayISO(),validUntil:plusDaysISO(30),status:'draft',currency:state.settings.currency,taxRate:state.settings.taxRate,lines:[{description:'',qty:1,rate:0}],notes:''};modalTitle.textContent=existing?`Edit ${estimate.number}`:'New estimate';modalBody.innerHTML=`<div class="form-grid"><div class="field"><label>Customer *</label><select class="select" name="customerId">${state.customers.map(c=>`<option value="${c.id}" ${c.id===estimate.customerId?'selected':''}>${esc(c.name)}</option>`).join('')}</select></div><div class="field"><label>Estimate number *</label><input class="input" name="number" required value="${esc(estimate.number)}"></div><div class="field"><label>Issue date</label><input class="input" name="issueDate" type="date" value="${esc(estimate.issueDate)}"></div><div class="field"><label>Valid until</label><input class="input" name="validUntil" type="date" value="${esc(estimate.validUntil)}"></div><div class="field"><label>Status</label><select class="select" name="status">${['draft','sent','accepted','declined'].map(s=>`<option value="${s}" ${estimate.status===s?'selected':''}>${s[0].toUpperCase()+s.slice(1)}</option>`).join('')}</select></div><div class="field"><label>Currency</label><select class="select" name="currency">${['USD','EUR','GBP','NOK','SEK','DKK'].map(c=>`<option ${c===estimate.currency?'selected':''}>${c}</option>`).join('')}</select></div><div class="field full"><div class="split"><label>Line items</label><button class="mini-btn" type="button" id="addLine">+ Line</button></div><div class="estimate-lines" id="estimateLines">${estimate.lines.map(lineMarkup).join('')}</div></div><div class="field"><label>Tax / VAT %</label><input class="input" name="taxRate" type="number" min="0" max="100" step="0.01" value="${Number(estimate.taxRate)||0}"></div><div class="field full"><label>Notes</label><textarea class="textarea" name="notes">${esc(estimate.notes||'')}</textarea></div><div class="field full"><div class="estimate-summary" id="estimateSummary"></div></div></div>`;const linesEl=modalBody.querySelector('#estimateLines'),summary=modalBody.querySelector('#estimateSummary');const updateTotals=()=>{const lines=[...linesEl.querySelectorAll('.estimate-line')].map(row=>({qty:Number(row.querySelector('[name="qty"]').value)||0,rate:Number(row.querySelector('[name="rate"]').value)||0}));const totals=calculateInvoice({lines,taxRate:Number(modalBody.querySelector('[name="taxRate"]').value)||0}),currency=modalBody.querySelector('[name="currency"]').value;summary.innerHTML=`<div><span class="muted">Subtotal</span><strong>${money(totals.subtotal,currency)}</strong></div><div><span class="muted">Tax</span><strong>${money(totals.tax,currency)}</strong></div><div class="grand"><span>Total</span><strong>${money(totals.total,currency)}</strong></div>`};const bindLines=()=>{linesEl.querySelectorAll('.remove-line').forEach(b=>b.onclick=()=>{if(linesEl.children.length>1)b.closest('.estimate-line').remove();updateTotals()});linesEl.querySelectorAll('input').forEach(i=>i.oninput=updateTotals)};modalBody.querySelector('#addLine').onclick=()=>{linesEl.insertAdjacentHTML('beforeend',lineMarkup());bindLines();updateTotals()};modalBody.querySelector('[name="taxRate"]').oninput=updateTotals;modalBody.querySelector('[name="currency"]').onchange=updateTotals;bindLines();updateTotals();modalAction=async()=>{const form=new FormData(modalForm),number=String(form.get('number')||'').trim();if(!number)return false;const lines=[...linesEl.querySelectorAll('.estimate-line')].map(row=>({description:row.querySelector('[name="description"]').value.trim()||'Service',qty:Number(row.querySelector('[name="qty"]').value)||1,rate:Number(row.querySelector('[name="rate"]').value)||0}));const draft={...(existing||{}),customerId:String(form.get('customerId')),number,issueDate:String(form.get('issueDate')||todayISO()),validUntil:String(form.get('validUntil')||''),status:String(form.get('status')||'draft'),currency:String(form.get('currency')||state.settings.currency||'USD'),taxRate:Number(form.get('taxRate'))||0,lines,notes:String(form.get('notes')||'').trim()};const saved=await saveEstimate(session.user.id,draft);if(existing)state.estimates=state.estimates.map(x=>x.id===existing.id?saved:x);else state.estimates.unshift(saved);render();return true};modal.showModal();}

newEstimateButton.onclick=()=>hasProAccess()&&canWrite()&&openEstimateModal();
modalForm.addEventListener('submit',async e=>{if(e.submitter?.value==='cancel')return;e.preventDefault();try{const ok=await modalAction?.();if(ok)modal.close()}catch(error){showError(error)}});
window.addEventListener('solobizkit:workspace-updated',()=>{if(session)hydrate().catch(showError)});
onAuthChange(async(_event,next)=>{session=next;if(session){try{await hydrate()}catch(error){showError(error)}}else{subscription=null;workspaceContext=null;state={customers:[],invoices:[],estimates:[],settings:{currency:'USD',taxRate:0,estimatePrefix:'EST-'}};renderAuth()}});
(async()=>{try{session=await getSession();if(session)await hydrate();else renderAuth()}catch(error){showError(error);renderAuth()}})();
