import { getDataOwnerId, getSession, getWorkspaceContext, loadWorkspace, supabase } from '../backend.js';

const app=document.querySelector('#app'),authActions=document.querySelector('#authActions'),shell=document.querySelector('#shell'),modal=document.querySelector('#dealModal'),form=document.querySelector('#dealForm'),modalTitle=document.querySelector('#dealModalTitle');
let session=null,context=null,dataOwner=null,workspace=null,deals=[],tasks=[],report=null,editing=null;
const stages=[['new','New lead'],['contacted','Contacted'],['estimate','Estimate'],['negotiation','Negotiation'],['won','Won'],['lost','Lost']];
const lossReasons=[['price','Price'],['no_response','No response'],['competitor','Competitor'],['timing','Timing'],['not_fit','Not a fit'],['other','Other']];
const esc=(v='')=>String(v).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'})[c]);
const today=()=>new Date().toISOString().slice(0,10);
const lang=()=>{try{return ['en','no','sv','de','es','fr'].includes(localStorage.getItem('sbk_language'))?localStorage.getItem('sbk_language'):'en'}catch{return'en'}};
const T={en:{open:'Open deals',value:'Pipeline value',hot:'Hot / warm leads',overdue:'Overdue tasks',forecast:'Weighted forecast',win:'Win rate',close:'Avg. days to close',month:'Won / lost · 30d',loss:'Top loss reason',report:'Sales intelligence',search:'Search deals or customers…',allCurrency:'All currencies',allScore:'All lead scores',refresh:'Refresh signals',deal:'+ Deal',noDeals:'No deals',viewed:'Estimate viewed',last:'last',due:'Due',edit:'Open',stage:'Stage',readonly:'Read-only workspace access. You can view scores and pipeline signals, but not move or edit deals.',leadScore:'Lead score',leadInfo:'Based on pipeline stage, estimate views, recent engagement, CRM activity and opportunity value.'},no:{open:'Åpne deals',value:'Pipelineverdi',hot:'Varme leads',overdue:'Forfalte oppgaver',forecast:'Vektet prognose',win:'Vinnrate',close:'Snitt dager til avslutning',month:'Vunnet / tapt · 30d',loss:'Vanligste tapsårsak',report:'Salgsinnsikt',search:'Søk i deals eller kunder…',allCurrency:'Alle valutaer',allScore:'Alle lead-scorer',refresh:'Oppdater signaler',deal:'+ Deal',noDeals:'Ingen deals',viewed:'Tilbud åpnet',last:'sist',due:'Forfall',edit:'Åpne',stage:'Fase',readonly:'Skrivebeskyttet tilgang. Du kan se score og pipeline, men ikke flytte eller redigere deals.',leadScore:'Lead score',leadInfo:'Basert på pipelinefase, åpning av tilbud, nylig engasjement, CRM-aktivitet og mulighetsverdi.'},sv:{open:'Öppna deals',value:'Pipelinevärde',hot:'Varma leads',overdue:'Förfallna uppgifter',forecast:'Viktad prognos',win:'Vinstgrad',close:'Snittdagar till avslut',month:'Vunna / förlorade · 30d',loss:'Vanligaste förlustorsak',report:'Försäljningsinsikt',search:'Sök deals eller kunder…',allCurrency:'Alla valutor',allScore:'Alla lead-poäng',refresh:'Uppdatera signaler',deal:'+ Deal',noDeals:'Inga deals',viewed:'Offert öppnad',last:'senast',due:'Förfall',edit:'Öppna',stage:'Fas',readonly:'Skrivskyddad åtkomst. Du kan se poäng och pipeline men inte flytta eller redigera deals.',leadScore:'Lead score',leadInfo:'Baserat på pipelinefas, offertöppningar, senaste engagemang, CRM-aktivitet och affärsvärde.'},de:{open:'Offene Deals',value:'Pipeline-Wert',hot:'Heiße / warme Leads',overdue:'Überfällige Aufgaben',forecast:'Gewichtete Prognose',win:'Gewinnrate',close:'Ø Tage bis Abschluss',month:'Gewonnen / verloren · 30T',loss:'Häufigster Verlustgrund',report:'Vertriebsanalyse',search:'Deals oder Kunden suchen…',allCurrency:'Alle Währungen',allScore:'Alle Lead-Scores',refresh:'Signale aktualisieren',deal:'+ Deal',noDeals:'Keine Deals',viewed:'Angebot geöffnet',last:'zuletzt',due:'Fällig',edit:'Öffnen',stage:'Phase',readonly:'Nur-Lese-Zugriff. Pipeline und Scores sind sichtbar, Deals können nicht bearbeitet werden.',leadScore:'Lead Score',leadInfo:'Basierend auf Pipeline-Phase, Angebotsaufrufen, Engagement, CRM-Aktivität und Deal-Wert.'},es:{open:'Deals abiertos',value:'Valor del pipeline',hot:'Leads calientes / templados',overdue:'Tareas vencidas',forecast:'Previsión ponderada',win:'Tasa de cierre',close:'Días medios al cierre',month:'Ganados / perdidos · 30d',loss:'Motivo de pérdida principal',report:'Inteligencia comercial',search:'Buscar deals o clientes…',allCurrency:'Todas las monedas',allScore:'Todas las puntuaciones',refresh:'Actualizar señales',deal:'+ Deal',noDeals:'Sin deals',viewed:'Presupuesto abierto',last:'último',due:'Vence',edit:'Abrir',stage:'Etapa',readonly:'Acceso de solo lectura. Puedes ver pipeline y puntuaciones, pero no editar deals.',leadScore:'Lead score',leadInfo:'Basado en etapa, aperturas de presupuestos, interacción, actividad CRM y valor.'},fr:{open:'Deals ouverts',value:'Valeur du pipeline',hot:'Leads chauds / tièdes',overdue:'Tâches en retard',forecast:'Prévision pondérée',win:'Taux de gain',close:'Jours moyens avant clôture',month:'Gagnés / perdus · 30j',loss:'Motif de perte principal',report:'Analyse commerciale',search:'Rechercher deals ou clients…',allCurrency:'Toutes les devises',allScore:'Tous les scores',refresh:'Actualiser les signaux',deal:'+ Deal',noDeals:'Aucun deal',viewed:'Devis ouvert',last:'dernier',due:'Échéance',edit:'Ouvrir',stage:'Étape',readonly:'Accès en lecture seule. Vous pouvez voir le pipeline et les scores, sans modifier les deals.',leadScore:'Lead score',leadInfo:'Basé sur l’étape, les ouvertures de devis, l’engagement, l’activité CRM et la valeur.'}};
const t=k=>T[lang()]?.[k]||T.en[k]||k;
function money(v,c='USD'){try{return new Intl.NumberFormat(undefined,{style:'currency',currency:c,maximumFractionDigits:0}).format(Number(v)||0)}catch{return `${c} ${Number(v||0).toFixed(0)}`}}
function currencySummary(rows,key='value'){return (rows||[]).map(x=>money(x[key],x.currency)).join(' · ')||'—'}
function customer(id){return workspace?.customers?.find(c=>c.id===id)||null}
function taskForDeal(id){return tasks.find(x=>x.deal_id===id&&!x.completed_at)||null}
function scoreBand(s){const n=Number(s||0);return n>=75?['hot','Hot']:n>=45?['warm','Warm']:['cold','Cold']}
function scoreTitle(d){const r=Array.isArray(d.score_reasons)?d.score_reasons:[];return `${Number(d.lead_score||0)}/100${r.length?' · '+r.join(' · '):''}`}
async function syncAutomation(){if(context?.canWrite){const{error}=await supabase.rpc('sync_crm_automation');if(error)console.warn('CRM automation sync skipped',error)}}
async function fetchRecords(){const[d,ts,r]=await Promise.all([supabase.from('crm_deals').select('*').eq('user_id',dataOwner).order('lead_score',{ascending:false}).order('updated_at',{ascending:false}),supabase.from('crm_tasks').select('*').eq('user_id',dataOwner).order('due_date',{ascending:true}),supabase.rpc('get_crm_pipeline_report')]);if(d.error)throw d.error;if(ts.error)throw ts.error;deals=d.data||[];tasks=ts.data||[];report=r.error?null:r.data}
async function load(){session=await getSession();if(!session?.user?.id){renderAuth();return}context=await getWorkspaceContext(true);dataOwner=await getDataOwnerId();workspace=await loadWorkspace(session.user.id);shell.classList.remove('signed-out');authActions.innerHTML=`<span class="account-chip"><strong>${esc(context?.name||session.user.email||'Account')}</strong><small>${esc(String(context?.role||'PRO').toUpperCase())}</small></span>`;await syncAutomation();await fetchRecords();render()}
function renderAuth(){app.innerHTML='<div class="auth-stage"><section class="auth-card"><p class="eyebrow">SOLOBIZKIT PRO</p><h2>Sign in from the main Pro workspace.</h2><a class="btn primary" href="/pro/">Open Pro</a></section></div>'}
function sumByCurrency(items){const m=new Map();for(const d of items)m.set(d.currency||'USD',(m.get(d.currency||'USD')||0)+Number(d.amount||0));return[...m.entries()].map(([c,v])=>money(v,c)).join(' · ')||'—'}
function lossLabel(reason){return lossReasons.find(x=>x[0]===reason)?.[1]||reason||'—'}
function reportBlock(){if(!report)return'';const top=(report.lostReasons||[])[0];return `<section class="pipeline-report"><div class="pipeline-report-head"><strong>${esc(t('report'))}</strong></div><div class="pipeline-report-grid"><div><small>${esc(t('forecast'))}</small><b>${esc(currencySummary(report.pipelineByCurrency,'weighted'))}</b></div><div><small>${esc(t('win'))}</small><b>${Number(report.winRate||0)}%</b></div><div><small>${esc(t('close'))}</small><b>${Number(report.avgCloseDays||0)}</b></div><div><small>${esc(t('month'))}</small><b>${Number(report.won30||0)} / ${Number(report.lost30||0)}</b></div><div><small>${esc(t('loss'))}</small><b>${esc(top?`${lossLabel(top.reason)} (${top.count})`:'—')}</b></div></div></section>`}
function render(){const active=deals.filter(d=>!['won','lost'].includes(d.stage)),hot=active.filter(d=>Number(d.lead_score||0)>=75),warm=active.filter(d=>Number(d.lead_score||0)>=45&&Number(d.lead_score||0)<75),overdue=tasks.filter(x=>!x.completed_at&&x.due_date<today());app.innerHTML=`<div class="pipeline-summary"><div class="stat"><div class="label">${t('open')}</div><div class="value">${active.length}</div></div><div class="stat"><div class="label">${t('value')}</div><div class="value compact-value">${esc(sumByCurrency(active))}</div></div><div class="stat"><div class="label">${t('hot')}</div><div class="value">${hot.length} / ${warm.length}</div></div><div class="stat ${overdue.length?'attention':''}"><div class="label">${t('overdue')}</div><div class="value">${overdue.length}</div></div></div>${reportBlock()}<section class="card"><div class="pipeline-toolbar"><div class="left"><input id="pipelineSearch" class="input search" type="search" placeholder="${esc(t('search'))}"><select id="pipelineCurrency" class="select"><option value="">${esc(t('allCurrency'))}</option>${[...new Set(deals.map(d=>d.currency).filter(Boolean))].sort().map(c=>`<option>${esc(c)}</option>`).join('')}</select><select id="pipelineScore" class="select"><option value="">${esc(t('allScore'))}</option><option value="hot">Hot 75+</option><option value="warm">Warm 45–74</option><option value="cold">Cold 0–44</option></select></div>${context?.canWrite?`<div class="pipeline-actions"><button class="btn secondary" id="refreshSignals">${esc(t('refresh'))}</button><button class="btn primary" id="newDeal">${esc(t('deal'))}</button></div>`:''}</div>${context?.canWrite?'':`<div class="pipeline-readonly">${esc(t('readonly'))}</div>`}<div class="pipeline-intel-note"><strong>${esc(t('leadScore'))}</strong><span>${esc(t('leadInfo'))}</span></div><div id="pipelineBoard" class="pipeline-board"></div></section>`;bindFilters();renderBoard();document.querySelector('#newDeal')?.addEventListener('click',()=>openDeal());document.querySelector('#refreshSignals')?.addEventListener('click',async e=>{e.currentTarget.disabled=true;try{await syncAutomation();await fetchRecords();render()}catch(err){alert(err.message||'Could not refresh signals.')}})}
function filtered(){const q=document.querySelector('#pipelineSearch')?.value.trim().toLowerCase()||'',c=document.querySelector('#pipelineCurrency')?.value||'',s=document.querySelector('#pipelineScore')?.value||'';return deals.filter(d=>!c||d.currency===c).filter(d=>!s||scoreBand(d.lead_score)[0]===s).filter(d=>{const x=customer(d.customer_id);return `${d.title} ${x?.name||''} ${x?.company||''}`.toLowerCase().includes(q)})}
function renderBoard(){const board=document.querySelector('#pipelineBoard');if(!board)return;const items=filtered();board.innerHTML=stages.map(([key,label])=>{const rows=items.filter(d=>d.stage===key);return`<section class="pipeline-column" data-stage="${key}"><div class="pipeline-column-head"><h3>${esc(label)}</h3><span>${rows.length} · ${esc(sumByCurrency(rows))}</span></div><div class="deal-list">${rows.length?rows.map(dealCard).join(''):`<div class="pipeline-empty">${esc(t('noDeals'))}</div>`}</div></section>`}).join('');bindBoard()}
function dealCard(d){const c=customer(d.customer_id),task=taskForDeal(d.id),overdue=task&&task.due_date<today(),[band,label]=scoreBand(d.lead_score),views=Number(d.engagement_count||0);return`<article class="deal-card" data-deal-id="${d.id}" draggable="${context?.canWrite?'true':'false'}"><div class="deal-card-top"><h4>${esc(d.title)}</h4><span class="lead-score ${band}" title="${esc(scoreTitle(d))}">${label} ${Number(d.lead_score||0)}</span></div><p>${esc(c?.name||'No customer')}${c?.company?` · ${esc(c.company)}`:''}</p><div class="deal-meta"><span class="deal-value">${esc(money(d.amount,d.currency))}</span><span>${Number(d.probability||0)}%</span></div>${views?`<div class="deal-signal">${esc(t('viewed'))} <strong>${views}×</strong>${d.last_engaged_at?` · ${esc(t('last'))} ${esc(new Date(d.last_engaged_at).toLocaleString())}`:''}</div>`:''}${d.stage==='lost'&&d.lost_reason?`<div class="deal-signal">Lost: ${esc(lossLabel(d.lost_reason))}</div>`:''}${d.next_action?`<div class="deal-action">${esc(d.next_action)}</div>`:''}${task?`<span class="task-due ${overdue?'overdue':''}">${overdue?'Overdue':t('due')} ${esc(task.due_date)}</span>`:''}${context?.canWrite?`<div class="deal-action pipeline-card-actions"><button class="mini-btn" data-edit-deal>${esc(t('edit'))}</button><select class="select pipeline-stage-select" data-stage-select aria-label="${esc(t('stage'))}">${stages.map(([k,l])=>`<option value="${k}" ${d.stage===k?'selected':''}>${esc(l)}</option>`).join('')}</select></div>`:''}</article>`}
function bindFilters(){document.querySelector('#pipelineSearch')?.addEventListener('input',renderBoard);document.querySelector('#pipelineCurrency')?.addEventListener('change',renderBoard);document.querySelector('#pipelineScore')?.addEventListener('change',renderBoard)}
function bindBoard(){document.querySelectorAll('[data-edit-deal]').forEach(b=>b.onclick=e=>{e.stopPropagation();openDeal(e.currentTarget.closest('[data-deal-id]').dataset.dealId)});document.querySelectorAll('[data-stage-select]').forEach(s=>s.onchange=e=>{e.stopPropagation();moveDeal(e.currentTarget.closest('[data-deal-id]').dataset.dealId,e.currentTarget.value,true)});if(!context?.canWrite)return;document.querySelectorAll('.deal-card').forEach(card=>card.addEventListener('dragstart',e=>e.dataTransfer.setData('text/plain',card.dataset.dealId)));document.querySelectorAll('.pipeline-column').forEach(col=>{col.addEventListener('dragover',e=>{e.preventDefault();col.classList.add('drag-over')});col.addEventListener('dragleave',()=>col.classList.remove('drag-over'));col.addEventListener('drop',async e=>{e.preventDefault();col.classList.remove('drag-over');await moveDeal(e.dataTransfer.getData('text/plain'),col.dataset.stage,true)})})}
async function chooseLossReason(){const reason=prompt('Why was this deal lost?\nprice · no_response · competitor · timing · not_fit · other','other');if(reason===null)return null;return lossReasons.some(x=>x[0]===reason)?reason:'other'}
async function syncCustomerStage(d,stage){if(!d.customer_id)return;const crm={new:'lead',contacted:'active',estimate:'active',negotiation:'active',won:'won',lost:'lost'}[stage];if(!crm)return;await supabase.from('customers').update({crm_enabled:true,crm_status:crm,crm_updated_at:new Date().toISOString()}).eq('id',d.customer_id).eq('user_id',dataOwner)}
async function moveDeal(id,stage,askLoss=false){const d=deals.find(x=>x.id===id);if(!d||d.stage===stage)return;let lost_reason=null;if(stage==='lost'&&askLoss){lost_reason=await chooseLossReason();if(lost_reason===null){renderBoard();return}}const probability={new:10,contacted:30,estimate:60,negotiation:80,won:100,lost:0}[stage],payload={stage,probability,updated_at:new Date().toISOString(),lost_reason:stage==='lost'?(lost_reason||'other'):null,lost_note:null};const{error}=await supabase.from('crm_deals').update(payload).eq('id',id).eq('user_id',dataOwner);if(error)return alert(error.message);await syncCustomerStage(d,stage);await fetchRecords();render()}
function openDeal(id=null){if(!context?.canWrite)return;editing=deals.find(d=>d.id===id)||null;modalTitle.textContent=editing?'Edit deal':'New deal';const score=editing?`<div class="field full"><div class="deal-score-detail"><strong>Lead score ${Number(editing.lead_score||0)}/100</strong><span>${esc((Array.isArray(editing.score_reasons)?editing.score_reasons:[]).join(' · ')||'Score updates automatically from CRM activity.')}</span></div></div>`:'';form.innerHTML=`<div class="form-grid">${score}<div class="field full"><label>Deal name</label><input class="input" name="title" required value="${esc(editing?.title||'')}"></div><div class="field"><label>Customer</label><select class="select" name="customer_id"><option value="">No customer</option>${workspace.customers.map(c=>`<option value="${c.id}" ${editing?.customer_id===c.id?'selected':''}>${esc(c.name)}</option>`).join('')}</select></div><div class="field"><label>Stage</label><select class="select" name="stage">${stages.map(([k,l])=>`<option value="${k}" ${editing?.stage===k?'selected':''}>${esc(l)}</option>`).join('')}</select></div><div class="field"><label>Amount</label><input class="input" name="amount" type="number" min="0" step="0.01" value="${Number(editing?.amount||0)}"></div><div class="field"><label>Currency</label><input class="input" name="currency" maxlength="3" value="${esc(editing?.currency||'USD')}"></div><div class="field"><label>Probability %</label><input class="input" name="probability" type="number" min="0" max="100" value="${Number(editing?.probability??10)}"></div><div class="field"><label>Follow-up date</label><input class="input" name="follow_up_date" type="date" value="${esc(editing?.follow_up_date||'')}"></div><div class="field full"><label>Next action</label><input class="input" name="next_action" value="${esc(editing?.next_action||'')}"></div><div class="field loss-field"><label>Lost reason</label><select class="select" name="lost_reason">${lossReasons.map(([k,l])=>`<option value="${k}" ${editing?.lost_reason===k?'selected':''}>${esc(l)}</option>`).join('')}</select></div><div class="field loss-field"><label>Lost note</label><input class="input" name="lost_note" value="${esc(editing?.lost_note||'')}"></div><div class="field full"><label>Source</label><input class="input" name="source" value="${esc(editing?.source||'')}"></div></div><div class="modal-actions"><button class="btn secondary" value="cancel">Cancel</button><button class="btn primary" value="default">Save deal</button></div>`;const toggle=()=>form.querySelectorAll('.loss-field').forEach(x=>x.hidden=form.elements.stage.value!=='lost');form.elements.stage.onchange=toggle;toggle();modal.showModal()}
form.addEventListener('submit',async e=>{if(e.submitter?.value==='cancel')return;e.preventDefault();if(!context?.canWrite)return;const fd=new FormData(form),stage=String(fd.get('stage')||'new'),payload={user_id:dataOwner,customer_id:String(fd.get('customer_id')||'')||null,title:String(fd.get('title')||'').trim(),stage,amount:Number(fd.get('amount')||0),currency:String(fd.get('currency')||'USD').trim().toUpperCase(),probability:Number(fd.get('probability')||0),follow_up_date:String(fd.get('follow_up_date')||'')||null,next_action:String(fd.get('next_action')||'').trim()||null,source:String(fd.get('source')||'').trim()||null,lost_reason:stage==='lost'?String(fd.get('lost_reason')||'other'):null,lost_note:stage==='lost'?(String(fd.get('lost_note')||'').trim()||null):null,updated_at:new Date().toISOString()};let r=editing?await supabase.from('crm_deals').update(payload).eq('id',editing.id).eq('user_id',dataOwner).select().single():await supabase.from('crm_deals').insert(payload).select().single();if(r.error)return alert(r.error.message);if(payload.follow_up_date){const{error}=await supabase.rpc('save_crm_followup_task',{p_deal_id:r.data.id,p_due_date:payload.follow_up_date,p_title:payload.next_action||`Follow up on ${payload.title}`});if(error)console.warn('Follow-up task could not be saved',error)}await syncCustomerStage(r.data,stage);modal.close();editing=null;await load()});
function style(){if(document.querySelector('#pipelineIntelStyle'))return;const s=document.createElement('style');s.id='pipelineIntelStyle';s.textContent='.pipeline-report{margin:0 0 18px;padding:14px;border:1px solid #e6eaf0;border-radius:16px;background:#fff}.pipeline-report-head{margin-bottom:10px}.pipeline-report-grid{display:grid;grid-template-columns:repeat(5,minmax(0,1fr));gap:10px}.pipeline-report-grid>div{padding:10px;border-radius:12px;background:#f7f9fc}.pipeline-report-grid small,.pipeline-report-grid b{display:block}.pipeline-report-grid small{color:#6b7280;margin-bottom:4px}.pipeline-card-actions{display:flex;gap:8px;align-items:center}.pipeline-stage-select{display:none;min-width:120px;padding:5px 7px;font-size:11px}@media(max-width:900px){.pipeline-report-grid{grid-template-columns:repeat(2,minmax(0,1fr))}.pipeline-stage-select{display:block}}@media(max-width:600px){.pipeline-report-grid{grid-template-columns:1fr}}';document.head.appendChild(s)}
style();load().catch(err=>{console.error(err);app.innerHTML=`<div class="empty">Could not load pipeline: ${esc(err.message||'Unknown error')}</div>`});